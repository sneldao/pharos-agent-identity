{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && corepack enable && pnpm --filter '@ligis/web...' run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
