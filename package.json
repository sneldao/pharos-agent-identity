{
  "name": "ligis",
  "version": "0.1.0",
  "description": "Ligis — Identity & Verifiable Credentials Skill for the Pharos AI Agent ecosystem. Issue portable agent IDs (ERC-721), sign and verify EIP-712 capability credentials, rotate keys, and revoke. Built for the Pharos Skill-to-Agent Dual Cascade Hackathon (Phase 1).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ligis": "./dist/cli/index.js"
  },
  "main": "./dist/cli/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/cli/index.js",
    "dev": "tsx src/cli/index.ts",
    "mcp": "node --enable-source-maps dist/mcp/server.js",
    "mcp:dev": "tsx src/mcp/server.ts",
    "test": "bash scripts/forge.sh test -vvv",
    "test:ts": "node --import tsx --test test/ts/*.test.ts",
    "test:all": "bash scripts/forge.sh test -vvv && npm run test:ts",
    "test:coverage": "bash scripts/forge.sh coverage",
    "deploy:atlantic": "bash scripts/deploy.sh atlantic",
    "verify:atlantic": "bash scripts/verify.sh atlantic",
    "demo": "bash scripts/demo.sh",
    "format": "bash scripts/forge.sh fmt && prettier --write 'src/**/*.{ts,json,md}'",
    "clean": "rm -rf out cache broadcast dist"
  },
  "keywords": [
    "pharos",
    "agent",
    "skill",
    "identity",
    "credentials",
    "eip-712",
    "erc-721",
    "mcp",
    "ai-agent",
    "web3"
  ],
  "dependencies": {
    "@0gfoundation/0g-compute-ts-sdk": "^0.8.4",
    "@0gfoundation/0g-storage-ts-sdk": "^1.2.6",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ethers": "^6.13.1",
    "viem": "^2.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
